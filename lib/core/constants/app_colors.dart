import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF003334); // Dark Green
  static const Color secondary = Color(0xFFA3CB43); // Lime Green
  static const Color accent = Color(0xFF00A86B); // Lighter Green

  // Backgrounds
  static const Color background = Color(0xFF003334);
  static const Color splashBackground = Color(0xFF4CD964);
  static const Color surface = Color(0xFF1E2929);
  static const Color bottomNavBackground = Color(0xFF1E1E1E);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textTertiary = Colors.white54;

  // Others
  static const Color darkTeal = Color(0xFF002323);
  static const Color error = Colors.redAccent;
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;
}
